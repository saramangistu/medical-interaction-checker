<%- include('../partials/header', { title: "Home - Medical Interaction Checker", user: user }) %>

<main class="container">
    <% if (!user) { %>
        <div class="welcome-card">
            <h2>Welcome to Medical Interaction Checker</h2>
            <p>Please log in or register to use the system.</p>
            <div class="button-group">
                <a href="/login" class="btn-home">Login</a>
                <a href="/auth/register-patient" class="btn-home-secondary">Patient Register</a>
                <a href="/auth/register-doctor" class="btn-home-secondary">Doctor Register</a>
            </div>
        </div>
    <% } else { %>
        <div class="user-info-card">
            <div class="profile-pic">
                <img src="<%= user.profileImage || '/images/profile/avatar.png' %>" alt="Profile Picture">
            </div>
            <div class="welcome-text">
                <h2>Welcome, <strong><%= user.username %></strong></h2>
                <p>Role: <%= user.role %></p>
            </div>
        </div>

        <nav class="main-nav">
            <a href="/drug-check/form" class="btn-home">💊 Drug – Condition Interaction</a>
            <a href="/image-check/food" class="btn-home">🖼 Food - Condition Interaction Analysis</a>
            <% if (user.role === 'doctor') { %>
                <a href="/doctor/panel" class="btn-home">🩺 Doctor Panel</a>
            <% } %>
        </nav>

        <div class="logout">
            <form action="/auth/logout" method="GET">
                <button type="submit" class="btn-home-danger">Logout</button>
            </form>
        </div>
    <% } %>
</main>

<%- include('../partials/footer') %>